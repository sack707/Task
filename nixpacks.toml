[phases.setup]
nixPkgs = ["pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm db:generate", "pnpm run build"]

[start]
cmd = "pnpm --filter api db:push || true && pnpm --filter api start:prod"
